/** @type {import('jest').Config} */
const config = {
    testEnvironment: 'node',
    // Add other Jest configurations here if needed
    // For example, if you have specific module resolution issues:
    // moduleNameMapper: {
    //   '^@/(.*)$': '<rootDir>/src/$1',
    // },
};

export default config;
